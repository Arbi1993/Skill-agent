name: DentaFlow Action Test

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/dentaflow-action-test.yml"
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "GitHub Actions is available for DentaFlow deployment"
